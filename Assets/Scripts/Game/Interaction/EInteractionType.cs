namespace Game.Interaction
{
    public enum EInteractionType
    {
        /// <summary>
        /// Interact immediately when player is at interaction range
        /// </summary>
        Touch,
        /// <summary>
        /// Interact when player is at interaction range and presses down the interaction input key
        /// </summary>
        Input
    }
}
